@{
    ViewData["Title"] = "Koncilia Contratos - Inicio";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-body p-5">
                    <div class="text-center mb-4">
                        <h2 class="text-primary">Koncilia Contratos</h2>
                        <p class="text-muted">Sistema de Gestión de Contratos</p>
                    </div>
                    
                    <div class="text-center">
                        <a href="@Url.Action("Login", "Account")" class="btn btn-primary btn-lg me-3">
                            <i class="fas fa-sign-in-alt"></i> Iniciar Sesión
                        </a>
                        <a href="@Url.Action("Register", "Account")" class="btn btn-outline-primary btn-lg">
                            <i class="fas fa-user-plus"></i> Registrarse
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
